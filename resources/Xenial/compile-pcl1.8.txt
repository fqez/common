sudo sh -c 'cat<<EOF>/etc/apt/sources.list.d/jderobotTest.list
# for ubuntu 16.04 LTS (32/64 bit)

deb http://jderobot.org/jderoboTest xenial main
EOF'

wget http://jderobot.org/jderoboTest/jderobot-test.asc -O - | sudo apt-key add -

sudo apt-get update
sudo apt-get install cmake

sudo apt-get install vtk7

sudo apt-get install libeigen3-dev libflann-dev libboost-dev libqhull-dev libboost-system-dev libboost-filesystem-dev libboost-thread-dev libboost-date-time-dev libboost-iostreams-dev libboost-chrono-dev libboost-atomic-dev libboost-regex-dev libopenni2-dev libusb-1.0

sudo apt-get install libopenni2-dev

cmake -DCMAKE_BUILD_TYPE=Release ..

